@model VlasikhaPlavanieWebsite.ViewModels.ManageStagesViewModel

<link rel="stylesheet" href="~/css/manage-stages.css" asp-append-version="true" />

<div class="manage-stages-container">
	<h2>Управление этапами регистрации</h2>

	<h3>Создать новый этап</h3>

	<form asp-action="CreateStage" method="post">
		<div class="form-group">
			<label for="StageName">Название этапа</label>
			<input asp-for="NewStage.Name" class="form-control stage-input" />
			<span asp-validation-for="NewStage.Name" class="text-danger"></span>
		</div>

		<div class="form-group">
			<label for="RegistrationStartDate">Дата начала регистрации</label>
			<input asp-for="NewStage.RegistrationStartDate" class="form-control stage-input" type="datetime-local" />
			<span asp-validation-for="NewStage.RegistrationStartDate" class="text-danger"></span>
		</div>

		<h4>Выберите дисциплины и укажите дистанции</h4>

		<div class="disciplines-list">
			@foreach (var discipline in new List<string> { "Вольный стиль", "Брасс", "Баттерфляй", "На спине", "Комплексное плавание" })
			{
				<div class="discipline-item">
					<input type="checkbox" name="SelectedDisciplines" value="@discipline" class="discipline-checkbox" />
					<label>@discipline</label>
					<input type="text" name="DisciplineDistances[@discipline]" placeholder="Введите дистанции через ;"
						   class="form-control distance-input" disabled />
				</div>
			}
		</div>

		<div class="form-group">
			<label for="CompetitionDate">Дата проведения соревнований</label>
			<input asp-for="NewStage.CompetitionStartDate" class="form-control stage-input" type="date" />
			<span asp-validation-for="NewStage.CompetitionStartDate" class="text-danger"></span>
		</div>

		<div class="form-group">
			<label for="CompetitionAddress">Адрес соревнований</label>
			<input asp-for="NewStage.Address" class="form-control stage-input" type="text" placeholder="Введите адрес" />
			<span asp-validation-for="NewStage.Address" class="text-danger"></span>
		</div>

		<div class="form-group">
			<input type="submit" value="Создать этап" class="btn btn-primary" />
		</div>
	</form>

	<h3>Существующие этапы</h3>
	<div>
		@foreach (var stage in Model.Stages)
		{
			<div class="stage-item">
				<div>
					<div class="stage-name">@stage.Name</div>
					<div>
						@stage.RegistrationStartDate.ToString("yyyy-MM-dd HH:mm")
						-
						@(stage.RegistrationEndDate.HasValue ? stage.RegistrationEndDate.Value.ToString("yyyy-MM-dd HH:mm") : "Не завершен")
					</div>
					<div>@(stage.IsOpen ? "Открыта" : "Закрыта")</div>
				</div>
				<div class="stage-actions">
					@if (stage.IsOpen)
					{
						<form asp-action="ChangeStageStatus" method="post">
							<input type="hidden" name="id" value="@stage.Id" />
							<input type="hidden" name="isOpen" value="false" />
							<button type="submit" class="btn btn-danger">Закрыть</button>
						</form>
					}
					else
					{
						<form asp-action="ChangeStageStatus" method="post">
							<input type="hidden" name="id" value="@stage.Id" />
							<input type="hidden" name="isOpen" value="true" />
							<button type="submit" class="btn btn-success">Открыть</button>
						</form>
					}
				</div>
			</div>
		}
	</div>
</div>

<script>
	document.addEventListener("DOMContentLoaded", function () {
		document.querySelectorAll(".discipline-checkbox").forEach(function (checkbox) {
			checkbox.addEventListener("change", function () {
				var input = this.closest(".discipline-item").querySelector(".distance-input");
				input.disabled = !this.checked;
				if (!this.checked) {
					input.value = "";
				}
			});
		});
	});
</script>